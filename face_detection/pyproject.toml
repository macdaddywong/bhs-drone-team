[project]
name = "face-detection"
version = "0.1.0"
description = "Basic webcam face detection with OpenCV"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.10.0",
]
